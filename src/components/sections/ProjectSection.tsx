import ProjectSlider from "@/src/components/sections/ProjectSlider"

export default async function SkillsSection() {
    return (
        <div className={`min-h-screen bg-[#080808] text-white flex items-center justify-center p-8 mt-20 `}>
            <ProjectSlider/>
        </div>
    )
}

